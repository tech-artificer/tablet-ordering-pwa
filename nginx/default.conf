server {
    listen 443 ssl;
    server_name woosoo.local;

    location = /ca.crt {
        auth_request off;
        alias /etc/nginx/certs/ca.crt;
        default_type application/x-x509-ca-cert;
        add_header Content-Disposition 'attachment; filename="woosoo-ca.crt"' always;
    }
}
